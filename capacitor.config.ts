import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Conversatodo.app',
  appName: 'Conversatodo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
